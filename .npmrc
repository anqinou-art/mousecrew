# Public registry, pinned for this repo.
# A lockfile pointing at a regional mirror is both a weak location fingerprint and a
# broken install for anyone outside that region.
registry=https://registry.npmjs.org/
